{
  "name": "claude-deck",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20.12"
  },
  "workspaces": [
    "packages/server",
    "packages/ui"
  ],
  "scripts": {
    "dev": "concurrently -n server,ui -c blue,green \"npm -w packages/server run dev\" \"npm -w packages/ui run dev\"",
    "build": "npm -w packages/server run build && npm -w packages/ui run build",
    "start": "npm -w packages/server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "playwright": "^1.59.1",
    "typescript": "^5.7.3"
  }
}
